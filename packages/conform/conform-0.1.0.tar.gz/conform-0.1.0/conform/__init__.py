"""
# Conform

conform is a small command line utility to generate
and distribute config files.

It can generate config files using a variety of sources (JSON/YAML files, HTTP
endpoints) and publish to a variety of destinations (currently SSH only, but 
WinRM support in the future)
"""
