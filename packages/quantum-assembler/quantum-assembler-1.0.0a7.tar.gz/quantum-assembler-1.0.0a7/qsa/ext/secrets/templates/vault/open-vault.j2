#!/bin/sh
QUANTUM_DEPLOYMENT_ENV=${QUANTUM_DEPLOYMENT_ENV:-"staging"}
VAULT_PASSWORD_FILE=vault/$QUANTUM_DEPLOYMENT_ENV.gpg
if [ ! -f $VAULT_PASSWORD_FILE ]; then
  echo "No such vault: ${QUANTUM_DEPLOYMENT_ENV}"
fi
gpg --batch --use-agent --decrypt $VAULT_PASSWORD_FILE 2> /dev/null
exit $?

