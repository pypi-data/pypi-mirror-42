Boostrapping the documentation
-------------------------------

The following packages are needed in addition to libgs_ops:

::

    $ virtualenv _venv
    $ pip install -r requirements-doc.txt

.. note::

    Currently only python2.7 is supported. Support for python3 is coming.

